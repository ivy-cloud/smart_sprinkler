/*
 * PLACEHOLDER — Relay Gateway (ESP32B, optional)
 *
 * Target role: BT client -> forward CSV to STM32B
 * Merge from: pre_code/ESP32_TASK/heli_rc_blue/heli_rc_blue.ino
 *
 * TODO: skip if laptop receives BT directly
 */

void setup() {
  Serial.begin(115200);
  Serial.println("PLACEHOLDER: relay_gateway — not implemented");
}

void loop() {
  delay(1000);
}
